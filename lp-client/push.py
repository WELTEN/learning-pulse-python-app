#push.py

import datetime

now = datetime.datetime.now()
interval = 36*10**9 #1 hour in microseconds

#Loop trhough all the active users

	#If the 
	if (now - experience.startTime > interval):

		#Learning experience rating required

		#Push notification to the user
		
		#Start another experience if not rated? or block?

	else:

		#Learning is still happening, user cannot rate





